module.exports = {
    presets: ['module:metro-react-native-babel-preset'],
    plugins: [
      'module:react-native-dotenv', // Make sure this is included if you're using react-native-dotenv
    ],
  };
  